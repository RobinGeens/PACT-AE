"""Settings used in all 'sweep' experiments."""

# Launch multiple runs in parallel
RUN_PARALLEL = True
# Number of parallel jobs to run
N_JOBS = 8
# Don't re-run the same experiment if the result already exists
SKIP_IF_RESULT_EXISTS = True
# Clear intermediate results before re-starting the experiment
CLEAR_INCOMPLETE_RUNS = False
